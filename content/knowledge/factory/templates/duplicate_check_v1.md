# Duplicate check template v1 (private)

Before scaffolding, reject or skip items whose id/slug already appears earlier in the deterministic queue or in an existing public article/package fixture id collision.

Do not merge duplicates into a public record.
