name: Deploy Staging

on:
  push:
    branches: [develop]
  workflow_dispatch:
    inputs:
      commit_sha:
        description: Exact develop commit SHA to deploy; leave blank for current origin/develop
        required: false
        default: ""
      content_release_tag:
        description: Staging content release tag (default from RELEASE_CONTENT_STAGING.json)
        required: false
        default: ""
      ops_mode:
        description: Empty=normal deploy. restore_prod_v4_only=emergency production restore. restore_prod_then_staging_022=v4 then shared 022 content.
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: bhava-staging
  cancel-in-progress: false

jobs:
  ops-recover:
    if: ${{ github.event_name == 'workflow_dispatch' && (inputs.ops_mode == 'restore_prod_then_staging_022' || inputs.ops_mode == 'restore_prod_v4_only') }}
    runs-on: ubuntu-latest
    timeout-minutes: 50
    environment:
      name: staging
      url: https://staging.bhava.me
    steps:
      - uses: actions/checkout@v6

      - name: Configure pinned SSH
        uses: ./.github/actions/configure-pinned-ssh
        with:
          ssh_key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          known_hosts: ${{ secrets.VPS_KNOWN_HOSTS }}

      - name: Build production API hotfix image
        # Allows shared content mounts to include private 021+ packages while production max stays 20.
        run: |
          PROD_SHA=c25973d29c200e7a4911c61b4c8d6b652cda0076
          docker build -f deploy/ionos/Dockerfile.api -t "bhava-api:${PROD_SHA}" .
          docker save "bhava-api:${PROD_SHA}" | gzip -9 > "bhava-api-${PROD_SHA}.tar.gz"

      - name: Download content bundles
        env:
          GH_TOKEN: ${{ github.token }}
          OPS_MODE: ${{ inputs.ops_mode }}
        run: |
          gh release download bhava-content-001-020-v4 \
            --repo "${GITHUB_REPOSITORY}" \
            --pattern "bhava-content-001-020-v4.tar.gz" \
            --pattern "bhava-content-001-020-v4.tar.gz.sha256"
          sha256sum --check bhava-content-001-020-v4.tar.gz.sha256
          if [[ "$OPS_MODE" == "restore_prod_then_staging_022" ]]; then
            gh release download bhava-content-001-022-staging-v1 \
              --repo "${GITHUB_REPOSITORY}" \
              --pattern "bhava-content-001-022-staging-v1.tar.gz" \
              --pattern "bhava-content-001-022-staging-v1.tar.gz.sha256"
            sha256sum --check bhava-content-001-022-staging-v1.tar.gz.sha256
          fi

      - name: Transfer artifacts
        env:
          HOST: ${{ secrets.VPS_HOST }}
          PORT: ${{ secrets.VPS_PORT }}
          USER: ${{ secrets.VPS_USER }}
          OPS_MODE: ${{ inputs.ops_mode }}
        run: |
          PROD_SHA=c25973d29c200e7a4911c61b4c8d6b652cda0076
          scp -P "$PORT" \
            "bhava-api-${PROD_SHA}.tar.gz" \
            bhava-content-001-020-v4.tar.gz \
            bhava-content-001-020-v4.tar.gz.sha256 \
            "$USER@$HOST:/opt/bhava/incoming/"
          if [[ "$OPS_MODE" == "restore_prod_then_staging_022" ]]; then
            scp -P "$PORT" \
              bhava-content-001-022-staging-v1.tar.gz \
              bhava-content-001-022-staging-v1.tar.gz.sha256 \
              "$USER@$HOST:/opt/bhava/incoming/"
          fi

      - name: Recover shared host
        env:
          HOST: ${{ secrets.VPS_HOST }}
          PORT: ${{ secrets.VPS_PORT }}
          USER: ${{ secrets.VPS_USER }}
          OPS_MODE: ${{ inputs.ops_mode }}
        run: |
          PROD_SHA=c25973d29c200e7a4911c61b4c8d6b652cda0076
          V4_SHA="$(awk '{print $1}' bhava-content-001-020-v4.tar.gz.sha256)"
          STG_SHA=""
          if [[ -f bhava-content-001-022-staging-v1.tar.gz.sha256 ]]; then
            STG_SHA="$(awk '{print $1}' bhava-content-001-022-staging-v1.tar.gz.sha256)"
          fi
          ssh -p "$PORT" "$USER@$HOST" \
            "OPS_MODE='${OPS_MODE}' PROD_SHA='${PROD_SHA}' V4_SHA='${V4_SHA}' STG_SHA='${STG_SHA}' bash -s" <<'EOS'
          set -Eeuo pipefail
          cd /opt/bhava/config
          docker compose --env-file runtime.env stop web-staging api-staging || true
          docker load -i "/opt/bhava/incoming/bhava-api-${PROD_SHA}.tar.gz"
          BHAVA_PUBLIC_STORY_MAX=20 BHAVA_RECREATE_CONTENT_MOUNTS=0 \
            bash /opt/bhava/config/scripts/install-content-bundle.sh \
            /opt/bhava/incoming/bhava-content-001-020-v4.tar.gz \
            bhava-content-001-020-v4 "${V4_SHA}"
          docker compose --env-file runtime.env up -d --no-build --force-recreate --no-deps api-production web-production
          sleep 12
          docker compose --env-file runtime.env ps api-production
          docker compose --env-file runtime.env logs api-production --tail 40 || true
          if [[ "${OPS_MODE}" == "restore_prod_then_staging_022" && -n "${STG_SHA}" ]]; then
            bash /opt/bhava/config/scripts/update-runtime-env.sh /opt/bhava/config/runtime.env \
              BHAVA_PROD_WEB_VERSION=001-020-v4 \
              BHAVA_PROD_CONTENT_RELEASE=bhava-content-001-020-v4 \
              BHAVA_WEB_VERSION=001-020-v4 \
              BHAVA_CONTENT_RELEASE=bhava-content-001-020-v4 \
              BHAVA_STAGING_CONTENT_RELEASE=bhava-content-001-022-staging-v1 \
              BHAVA_STAGING_PUBLIC_STORY_MAX=22 \
              BHAVA_STAGING_WEB_VERSION=001-022-staging
            BHAVA_PUBLIC_STORY_MAX=22 BHAVA_RECREATE_CONTENT_MOUNTS=0 \
              bash /opt/bhava/config/scripts/install-content-bundle.sh \
              /opt/bhava/incoming/bhava-content-001-022-staging-v1.tar.gz \
              bhava-content-001-022-staging-v1 "${STG_SHA}"
            docker compose --env-file runtime.env up -d --no-build --force-recreate --no-deps api-production
            docker compose --env-file runtime.env up -d --no-build --force-recreate --no-deps api-staging web-staging
            sleep 12
          fi
          docker compose --env-file runtime.env ps || true
          EOS

      - name: Verify production restored
        run: |
          set -euo pipefail
          for i in $(seq 1 36); do
            code="$(curl -sS -o /tmp/bhava_ver.json -w '%{http_code}' https://bhava.me/api/v1/version || true)"
            if [[ "$code" == "200" ]]; then
              cat /tmp/bhava_ver.json
              python3 - <<'PY'
          import json
          data = json.load(open("/tmp/bhava_ver.json", encoding="utf-8"))
          assert int(data.get("public_story_max") or 0) == 20, data
          assert data.get("environment") == "production", data
          PY
              curl -fsS https://bhava.me/stories/020 >/dev/null
              code021="$(curl -sS -o /dev/null -w '%{http_code}' https://bhava.me/stories/021 || true)"
              [[ "$code021" == "404" ]]
              echo "production recovered"
              exit 0
            fi
            echo "waiting production version HTTP ${code} (${i}/36)"
            sleep 5
          done
          exit 1

      - name: Verify staging restored
        if: ${{ inputs.ops_mode == 'restore_prod_then_staging_022' }}
        env:
          STAGING_BASIC_AUTH_USER: ${{ secrets.STAGING_BASIC_AUTH_USER }}
          STAGING_BASIC_AUTH_PASSWORD: ${{ secrets.STAGING_BASIC_AUTH_PASSWORD }}
        run: |
          # Ops recovery only restores content/API hotfix; staging web SHA may already differ.
          # Confirm staging serves 022 and reports max 22 without requiring a specific web SHA.
          AUTH=(-u "${STAGING_BASIC_AUTH_USER}:${STAGING_BASIC_AUTH_PASSWORD}")
          curl -fsS "${AUTH[@]}" https://staging.bhava.me/api/v1/version | tee /tmp/stg_ver.json
          python3 - <<'PY'
          import json
          data = json.load(open("/tmp/stg_ver.json", encoding="utf-8"))
          assert int(data.get("public_story_max") or 0) == 22, data
          assert data.get("environment") == "staging", data
          PY
          code021="$(curl -sS "${AUTH[@]}" -o /dev/null -w '%{http_code}' https://staging.bhava.me/stories/021 || true)"
          code022="$(curl -sS "${AUTH[@]}" -o /dev/null -w '%{http_code}' https://staging.bhava.me/stories/022 || true)"
          code023="$(curl -sS "${AUTH[@]}" -o /dev/null -w '%{http_code}' https://staging.bhava.me/stories/023 || true)"
          [[ "$code021" == "200" && "$code022" == "200" && "$code023" == "404" ]]

  resolve:
    if: ${{ !(github.event_name == 'workflow_dispatch' && (inputs.ops_mode == 'restore_prod_then_staging_022' || inputs.ops_mode == 'restore_prod_v4_only')) }}
    runs-on: ubuntu-latest
    outputs:
      sha: ${{ steps.release.outputs.sha }}
      content_tag: ${{ steps.release.outputs.content_tag }}
      public_max: ${{ steps.release.outputs.public_max }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: develop

      - id: release
        env:
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED: ${{ inputs.commit_sha }}
          CONTENT_TAG_INPUT: ${{ inputs.content_release_tag }}
          GITHUB_SHA_ENV: ${{ github.sha }}
        run: |
          set -euo pipefail
          git fetch origin develop
          python3 - <<'PY'
          import json, os, subprocess
          from pathlib import Path

          staging_path = Path("deploy/content/RELEASE_CONTENT_STAGING.json")
          prod_path = Path("deploy/content/RELEASE_CONTENT.json")
          staging = json.loads(staging_path.read_text(encoding="utf-8")) if staging_path.is_file() else {}
          prod = json.loads(prod_path.read_text(encoding="utf-8"))
          # develop may already pin the upcoming 001-025 production release while
          # live production remains on an earlier pin until the promote deploy.
          assert int(prod["public_story_max"]) in (20, 22, 25), prod
          assert str(prod["tag"]).startswith("bhava-content-001-0"), prod
          if int(prod["public_story_max"]) == 25:
              assert str(prod["tag"]).startswith("bhava-content-001-025-"), prod
          elif int(prod["public_story_max"]) == 22:
              assert str(prod["tag"]).startswith("bhava-content-001-022-"), prod
          else:
              assert str(prod["tag"]).startswith("bhava-content-001-020-"), prod

          default_tag = staging.get("tag") or "bhava-content-001-025-staging-v1"
          default_max = int(staging.get("public_story_max") or 25)
          event = os.environ["EVENT_NAME"]
          requested = (os.environ.get("REQUESTED") or "").strip()
          content_input = (os.environ.get("CONTENT_TAG_INPUT") or "").strip()

          if event == "push":
              sha = os.environ["GITHUB_SHA_ENV"]
              content_tag = default_tag
          else:
              sha = requested or subprocess.check_output(
                  ["git", "rev-parse", "origin/develop"], text=True
              ).strip()
              content_tag = content_input or default_tag

          public_max = default_max
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as handle:
              handle.write(f"sha={sha}\n")
              handle.write(f"content_tag={content_tag}\n")
              handle.write(f"public_max={public_max}\n")
          print(f"Resolved staging deploy SHA={sha} content={content_tag} max={public_max}")
          PY

      - name: Verify resolved commit is on develop
        env:
          SHA: ${{ steps.release.outputs.sha }}
        run: |
          git cat-file -e "${SHA}^{commit}"
          git merge-base --is-ancestor "$SHA" origin/develop

  deploy:
    if: ${{ !(github.event_name == 'workflow_dispatch' && (inputs.ops_mode == 'restore_prod_then_staging_022' || inputs.ops_mode == 'restore_prod_v4_only')) }}
    needs: resolve
    runs-on: ubuntu-latest
    timeout-minutes: 50
    environment:
      name: staging
      url: https://staging.bhava.me
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.resolve.outputs.sha }}

      - name: Guard production content pin is governed
        run: |
          python3 - <<'PY'
          import json
          pin = json.load(open("deploy/content/RELEASE_CONTENT.json", encoding="utf-8"))
          assert int(pin["public_story_max"]) in (20, 22, 25), pin
          assert pin["tag"].startswith("bhava-content-001-0"), pin
          if int(pin["public_story_max"]) == 25:
              assert pin["tag"].startswith("bhava-content-001-025-"), pin
          elif int(pin["public_story_max"]) == 22:
              assert pin["tag"].startswith("bhava-content-001-022-"), pin
          else:
              assert pin["tag"].startswith("bhava-content-001-020-"), pin
          print("production pin OK", pin["tag"], pin["public_story_max"])
          PY

      - name: Build images
        env:
          RELEASE_SHA: ${{ needs.resolve.outputs.sha }}
          CONTENT_TAG: ${{ needs.resolve.outputs.content_tag }}
          PUBLIC_MAX: ${{ needs.resolve.outputs.public_max }}
        run: |
          git fetch origin main
          PROD_SHA="$(git rev-parse origin/main)"
          if [[ "${PUBLIC_MAX}" == "35" ]]; then
            STAGING_WEB_VERSION=001-035-staging
          elif [[ "${PUBLIC_MAX}" == "25" ]]; then
            STAGING_WEB_VERSION=001-025-staging
          elif [[ "${PUBLIC_MAX}" == "22" ]]; then
            STAGING_WEB_VERSION=001-022-staging
          else
            STAGING_WEB_VERSION=001-025-staging
          fi
          docker build -f deploy/ionos/Dockerfile.web \
            --build-arg RELEASE_SHA=${RELEASE_SHA} \
            --build-arg BHAVA_WEB_VERSION=${STAGING_WEB_VERSION} \
            --build-arg BHAVA_CONTENT_RELEASE=${CONTENT_TAG} \
            --build-arg NEXT_PUBLIC_BHAVA_PUBLIC_STORY_MAX=${PUBLIC_MAX} \
            -t bhava-web:${RELEASE_SHA} .
          docker build -f deploy/ionos/Dockerfile.api -t bhava-api:${RELEASE_SHA} .
          # Shared content mounts may include private packages above the live
          # production ceiling. Keep production's running image tag pinned to
          # origin/main while refreshing the API binary from this develop SHA.
          docker tag "bhava-api:${RELEASE_SHA}" "bhava-api:${PROD_SHA}"
          docker save bhava-web:${RELEASE_SHA} bhava-api:${RELEASE_SHA} "bhava-api:${PROD_SHA}" \
            | gzip -9 > bhava-images-${RELEASE_SHA}.tar.gz

      - name: Package deployment configuration
        env:
          RELEASE_SHA: ${{ needs.resolve.outputs.sha }}
        run: |
          tar -czf bhava-config-${RELEASE_SHA}.tar.gz \
            -C deploy/ionos \
            docker-compose.yml Caddyfile runtime.env.example scripts

      - name: Download approved staging content
        env:
          GH_TOKEN: ${{ github.token }}
          CONTENT_TAG: ${{ needs.resolve.outputs.content_tag }}
        run: |
          gh release download "$CONTENT_TAG" \
            --repo "${GITHUB_REPOSITORY}" \
            --pattern "${CONTENT_TAG}.tar.gz" \
            --pattern "${CONTENT_TAG}.tar.gz.sha256"
          sha256sum --check "${CONTENT_TAG}.tar.gz.sha256"
          if [[ -f deploy/content/RELEASE_CONTENT_STAGING.json ]]; then
            python3 - <<'PY'
          import hashlib, json
          pin = json.load(open("deploy/content/RELEASE_CONTENT_STAGING.json", encoding="utf-8"))
          tag = pin["tag"]
          expected = pin["sha256"].lower()
          actual = hashlib.sha256(open(f"{tag}.tar.gz", "rb").read()).hexdigest()
          assert actual == expected, (actual, expected)
          assert int(pin["public_story_max"]) >= 1
          print("staging content pin OK", tag, actual, "max", pin["public_story_max"])
          PY
          fi

      - name: Configure pinned SSH
        uses: ./.github/actions/configure-pinned-ssh
        with:
          ssh_key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          known_hosts: ${{ secrets.VPS_KNOWN_HOSTS }}

      - name: Transfer release
        env:
          HOST: ${{ secrets.VPS_HOST }}
          PORT: ${{ secrets.VPS_PORT }}
          USER: ${{ secrets.VPS_USER }}
          RELEASE_SHA: ${{ needs.resolve.outputs.sha }}
          CONTENT_TAG: ${{ needs.resolve.outputs.content_tag }}
        run: |
          scp -P "$PORT" \
            bhava-images-${RELEASE_SHA}.tar.gz \
            bhava-config-${RELEASE_SHA}.tar.gz \
            "${CONTENT_TAG}.tar.gz" \
            "${CONTENT_TAG}.tar.gz.sha256" \
            "$USER@$HOST:/opt/bhava/incoming/"

      - name: Deploy release
        env:
          HOST: ${{ secrets.VPS_HOST }}
          PORT: ${{ secrets.VPS_PORT }}
          USER: ${{ secrets.VPS_USER }}
          RELEASE_SHA: ${{ needs.resolve.outputs.sha }}
          CONTENT_TAG: ${{ needs.resolve.outputs.content_tag }}
          PUBLIC_MAX: ${{ needs.resolve.outputs.public_max }}
        run: |
          CONTENT_SHA="$(awk '{print $1}' "${CONTENT_TAG}.tar.gz.sha256")"
          git fetch origin main
          PROD_SHA="$(git rev-parse origin/main)"
          PROD_MAX="$(python -c 'import json; print(json.load(open("deploy/content/RELEASE_CONTENT.json",encoding="utf-8"))["public_story_max"])')"
          PROD_TAG="$(python -c 'import json; print(json.load(open("deploy/content/RELEASE_CONTENT.json",encoding="utf-8"))["tag"])')"
          if [[ "${PROD_MAX}" == "25" ]]; then
            PROD_WEB_VERSION=001-025-v1
          elif [[ "${PROD_MAX}" == "22" ]]; then
            PROD_WEB_VERSION=001-022-v1
          else
            PROD_WEB_VERSION=001-020-v4
          fi
          # Preserve the committed production ceiling/content pin. Shared mounts
          # may point at staging content briefly; production privacy depends on
          # BHAVA_PROD_PUBLIC_STORY_MAX matching RELEASE_CONTENT.json.
          ssh -p "$PORT" "$USER@$HOST" \
            "mkdir -p /opt/bhava/config && \
             tar -xzf /opt/bhava/incoming/bhava-config-${RELEASE_SHA}.tar.gz -C /opt/bhava/config && \
             chmod +x /opt/bhava/config/scripts/*.sh && \
             docker load -i /opt/bhava/incoming/bhava-images-${RELEASE_SHA}.tar.gz && \
             bash /opt/bhava/config/scripts/update-runtime-env.sh /opt/bhava/config/runtime.env \
               BHAVA_PROD_RELEASE_SHA=${PROD_SHA} \
               BHAVA_PROD_PUBLIC_STORY_MAX=${PROD_MAX} \
               BHAVA_PROD_WEB_VERSION=${PROD_WEB_VERSION} \
               BHAVA_PROD_CONTENT_RELEASE=${PROD_TAG} \
               BHAVA_WEB_VERSION=${PROD_WEB_VERSION} \
               BHAVA_CONTENT_RELEASE=${PROD_TAG} \
               BHAVA_STAGING_CONTENT_RELEASE=${CONTENT_TAG} \
               BHAVA_STAGING_PUBLIC_STORY_MAX=${PUBLIC_MAX} \
               BHAVA_STAGING_WEB_VERSION=$(if [[ "${PUBLIC_MAX}" == "35" ]]; then echo 001-035-staging; elif [[ "${PUBLIC_MAX}" == "22" ]]; then echo 001-022-staging; else echo 001-025-staging; fi) && \
             cd /opt/bhava/config && \
             docker compose --env-file runtime.env up -d --no-build --force-recreate --no-deps api-production && \
             sleep 8 && \
             BHAVA_PUBLIC_STORY_MAX=${PUBLIC_MAX} bash /opt/bhava/config/scripts/install-content-bundle.sh \
               /opt/bhava/incoming/${CONTENT_TAG}.tar.gz ${CONTENT_TAG} ${CONTENT_SHA} && \
             bash /opt/bhava/config/scripts/deploy-release.sh staging ${RELEASE_SHA} \
               /opt/bhava/incoming/bhava-images-${RELEASE_SHA}.tar.gz \
               /opt/bhava/incoming/bhava-config-${RELEASE_SHA}.tar.gz && \
             docker compose --env-file runtime.env up -d --no-build --force-recreate --no-deps api-production"

      - name: Staging TLS readiness
        env:
          STAGING_BASIC_AUTH_USER: ${{ secrets.STAGING_BASIC_AUTH_USER }}
          STAGING_BASIC_AUTH_PASSWORD: ${{ secrets.STAGING_BASIC_AUTH_PASSWORD }}
        run: bash deploy/ionos/scripts/tls-readiness.sh https://staging.bhava.me

      - name: Staging application smoke
        env:
          STAGING_BASIC_AUTH_USER: ${{ secrets.STAGING_BASIC_AUTH_USER }}
          STAGING_BASIC_AUTH_PASSWORD: ${{ secrets.STAGING_BASIC_AUTH_PASSWORD }}
          RELEASE_SHA: ${{ needs.resolve.outputs.sha }}
        run: |
          SKIP_TLS_READINESS=1 bash deploy/ionos/scripts/smoke-test.sh \
            https://staging.bhava.me "${RELEASE_SHA}" staging

      - name: Stage 1 private stories 026-035 route matrix
        if: ${{ needs.resolve.outputs.public_max >= 35 }}
        env:
          STAGING_BASIC_AUTH_USER: ${{ secrets.STAGING_BASIC_AUTH_USER }}
          STAGING_BASIC_AUTH_PASSWORD: ${{ secrets.STAGING_BASIC_AUTH_PASSWORD }}
          RELEASE_SHA: ${{ needs.resolve.outputs.sha }}
        run: |
          set -euo pipefail
          AUTH=(-u "${STAGING_BASIC_AUTH_USER}:${STAGING_BASIC_AUTH_PASSWORD}")
          BASE=https://staging.bhava.me
          ver="$(curl -fsS "${AUTH[@]}" "${BASE}/api/v1/version")"
          echo "$ver"
          echo "$ver" | grep -F "${RELEASE_SHA}" >/dev/null
          code_catalog="$(curl -sS -L "${AUTH[@]}" -o /dev/null -w '%{http_code}' "${BASE}/stories")"
          code_api_list="$(curl -sS "${AUTH[@]}" -o /dev/null -w '%{http_code}' "${BASE}/api/v1/stories")"
          echo "catalog_page=${code_catalog} catalog_api=${code_api_list}"
          [[ "$code_catalog" == "200" && "$code_api_list" == "200" ]]
          for n in $(seq 26 35); do
            sn="$(printf '%03d' "$n")"
            page="$(curl -sS -L "${AUTH[@]}" -o /dev/null -w '%{http_code}' "${BASE}/stories/${sn}")"
            api="$(curl -sS "${AUTH[@]}" -o /dev/null -w '%{http_code}' "${BASE}/api/v1/stories/${sn}")"
            audio="$(curl -sS "${AUTH[@]}" -o /dev/null -w '%{http_code}' "${BASE}/api/v1/stories/${sn}/assets/narration.mp3")"
            echo "${sn} page=${page} api=${api} audio=${audio}"
            if [[ "$page" != "200" || "$api" != "200" || "$audio" != "200" ]]; then
              echo "ROUTE_FAIL ${sn}" >&2
              exit 1
            fi
          done
          p036="$(curl -sS -L "${AUTH[@]}" -o /dev/null -w '%{http_code}' "${BASE}/stories/036")"
          a036="$(curl -sS "${AUTH[@]}" -o /dev/null -w '%{http_code}' "${BASE}/api/v1/stories/036")"
          echo "036 page=${p036} api=${a036}"
          [[ "$p036" == "404" && "$a036" == "404" ]]
          hdrs="$(curl -sSI "${AUTH[@]}" "${BASE}/stories/026")"
          echo "$hdrs" | grep -Eiq '^x-robots-tag:[[:space:]]*noindex'
          echo "STAGE1_ROUTE_MATRIX_PASS"