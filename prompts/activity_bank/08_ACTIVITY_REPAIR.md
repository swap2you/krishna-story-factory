# Activity Repair

Given a weak ActivityPack and QA issues, return a repaired ActivityPack JSON only.

Repair goals:

* strengthen story_connection on every page
* replace generic components with source-specific labels
* ensure 2–4 pages for normal packs (1–2 for simple types)
* add missing cut/fold/parent notes for crafts
* remove answer keys from printable surfaces
* diversify primary type when history forbids the current choice
* preserve child-safety and must_avoid boundaries
