# V1.7 lock ownership

Structured lock helpers live in `krishna_story_factory/pipeline_lock.py` with tests in `tests/test_v17_pipeline_lock.py` (shipped with the process-isolation commit). Validation uses `.pipeline.validate.lock`.
