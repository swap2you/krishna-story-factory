# 3D motion-lab artifacts pointer

Local, gitignored artifact root:

```text
work/3d-motion-lab/artifacts/
```

- Tracked contract: [CONTRACT.md](./CONTRACT.md)
- Gitignore: repository `/work/` (see root `.gitignore`)
- Do not place artifacts under `apps/web/public` or `content/`
