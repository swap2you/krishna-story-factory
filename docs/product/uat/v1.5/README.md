# Immutable SHA-bound UAT evidence helper

Creates `docs/product/uat/v1.5/runs/<timestamp>-<short-sha>/run-metadata.json`.

```powershell
.\scripts\write_bhava_v15_evidence_bundle.ps1 -PlaywrightExitCode 0
```
