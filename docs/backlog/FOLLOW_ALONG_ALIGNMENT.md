# Backlog — Follow-along alignment (D-09)

## Status

**Open / deferred.** Sentence-level follow-along cues remain `needs_alignment`.

## Scope

- Sync schema and UI already support alignment states (`aligned` | `needs_alignment` | `in_review`).  
- Published stories honestly report `needs_alignment` rather than fabricating timings.  
- Closing D-09 requires real narration-to-text alignment work.

## Out of scope for this release

- No paid transcription / alignment APIs in the current quality-completion / v3 staging candidate.  
- Do not invent or approximate cue timings to force `aligned`.

## When to pick up

Future backlog after Stories 001–020 content/release approval. Track as defect/backlog id **D-09**.
