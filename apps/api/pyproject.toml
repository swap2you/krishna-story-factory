[project]
name = "bhava-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic>=2",
  "sqlalchemy>=2",
  "alembic",
  "httpx",
  "python-multipart",
  "bleach",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
